<!DOCTYPE configuration PUBLIC "-//mybatis.org//DTD Config 3.0//EN" "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <typeAliases>
        <typeAlias alias="user" type="cn.qcode.entity.User"/>
    </typeAliases>

    <!-- 指定映射文件 -->
    <mappers>
        <mapper resource="mapper/User-mapping.xml" />
    </mappers>
</configuration>